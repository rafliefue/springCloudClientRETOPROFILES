spring:
  config:
    activate:
      on-profile:
      - dev
  application:
    name: myvalue 
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888   
---
spring:  config:    activate:      on-profile:      - prod
  application:
    name: test 
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888   